= simple_form_for(@task) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name

  .form-inputs
    = f.text_area :description

  .form-inputs
    = f.input :due_to, as: :datetime, html5: true

  .form-inputs
    = f.association :user

  .form-actions
    = f.button :submit
